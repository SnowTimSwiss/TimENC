# Rust
/target/
**/*.rs.bk
*.pdb
# Only ignore the root library crate's lockfile; the Tauri app crate's
# lockfile (src-tauri/Cargo.lock) is tracked for reproducible Flatpak builds.
/Cargo.lock

# Tauri
src-tauri/target/
**/target/
# NOTE: src-tauri/Cargo.lock is intentionally tracked (see below) so the
# Flatpak/Flathub build can vendor exact dependency versions offline.

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.AppImage
*.deb
*.dmg
*-portable-*.exe
*-Setup-*.exe

# Old Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# Testing
.pytest_cache/
.coverage
htmlcov/

# Legacy folder
legacy/dist/
legacy/build/

.claude